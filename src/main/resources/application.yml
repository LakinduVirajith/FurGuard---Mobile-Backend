server:
  port: 8080

base:
  url: http://localhost:8080

application:
  security:
    jwt:
      secret-key: 59ae2b35cca7b68cb0117872cf23ed15eb3cc668b7cd48c394d336b375fa0326
      expiration: 7200000 #TWO HOURS
      refresh-token:
        expiration: 2419200000 #ONE MONTH

spring:
  profiles:
    active: dev
  mail:
    host: smtp.gmail.com
    port: 587
    username: "YOUR EMAIL"
    password: "YOUR PASSWORD"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

---

spring:
  config:
    activate:
      on-profile: dev
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    url: jdbc:mysql://localhost:3306/fur_guard
    password: "YOUR PASSWORD"
  jpa:
    show-sql: 'true'
    hibernate:
      ddl-auto: update

---

spring:
  config:
    activate:
      on-profile: qa
  datasource:
    url: jdbc:mysql://localhost:3306/qa_db
    username: root
    password: Lakindu@MySQL
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    show-sql: true

---

spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: jdbc:mysql://localhost:3306/prod_db
    username: root
    password: Lakindu@MySQL
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    show-sql: true